import { IconSettings } from '@tabler/icons-react';
import './Settings.css'
export default function Settings({logo}){
    return(
        <div className="settings-container">
            {logo}
        </div>
    );
}